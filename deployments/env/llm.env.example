# LLM service settings.
# Stable key for encrypting LLM provider API keys in MongoDB.
# Must be at least 32 bytes. Changing it requires re-entering provider API keys.
LLM_ENCRYPT_KEY=replace-with-stable-32-byte-key
